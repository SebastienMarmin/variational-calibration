import os, sys

import vcal.layers
import vcal.utilities
import vcal.kernels
import vcal.nets
import vcal.stats
import vcal.vardl_utils
import vcal.learning


import logging

logging.getLogger(__name__).addHandler(logging.NullHandler())
